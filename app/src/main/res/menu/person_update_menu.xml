<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
  <item
      android:id="@+id/person_update_button"
      android:title="更新"
      app:showAsAction="always"
      />

  <item
      android:id="@+id/person_delete_button"
      android:title="删除"
      app:showAsAction="always"
      />
</menu>
